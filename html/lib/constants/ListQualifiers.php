<?php

abstract class ListQualifiers extends Enum {
  const CHEAP        = 1;
  const FIRST_DATE   = 2;
  const GROUP        = 3;
  const FAMILY       = 4;
  const ROMANTIC     = 6;
  const TAKEOUT      = 7;
  const VEG          = 8;
}